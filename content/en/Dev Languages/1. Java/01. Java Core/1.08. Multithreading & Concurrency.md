---
title: 1.08. Multithreading & Concurrency
---
[[ru/Dev Languages/1. Java/01. Java Core/1.08. Multithreading & Concurrency|RU]] | [[en/Dev Languages/1. Java/01. Java Core/1.08. Multithreading & Concurrency|EN]] | [[de/Dev Languages/1. Java/01. Java Core/1.08. Multithreading & Concurrency|DE]]
## Thread
```java
Thread t = new Thread(() -> {
    System.out.println("Hello from " + Thread.currentThread().getName());
});
t.start();
```
## ExecutorService
```java
ExecutorService executor = Executors.newFixedThreadPool(2);
executor.submit(() -> System.out.println("Task 1"));
executor.submit(() -> System.out.println("Task 2"));
executor.shutdown();
```
## CompletableFuture
```java
CompletableFuture.supplyAsync(() -> "Hello")
            .thenApply(s -> s + " World")
            .thenAccept(System.out::println);
```
## Synchronization
```java
class Counter {
    private int count = 0;
    public synchronized void increment() { count++; }
    public synchronized int get() { return count; }
}
```
## Synchronization

```java
class Counter {
    private int count = 0;
    public synchronized void increment() { count++; }
    public synchronized int get() { return count; }
}
```
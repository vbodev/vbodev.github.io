---
title: 1. PowerShell für die Windows-Server-Verwaltung – Beispiele
---
[[ru/Administration/3.Windows/Courses/1. Powershell for Windows Server Administration - Examples|RU]] | [[en/Administration/3.Windows/Courses/1. Powershell for Windows Server Administration - Examples|EN]] | [[de/Administration/3.Windows/Courses/1. Powershell for Windows Server Administration - Examples|DE]]

# Schutzwrapper: Beim Ausführen der Datei werden die Beispiele nicht automatisch ausgeführt.
# Kopieren Sie den benötigten Abschnitt in ein separates Skript oder eine interaktive Sitzung.
if ($false) {

#region Hilfe und Suche
Get-Command -Name '*Process*'
Get-Command -Noun Service
Get-Help Get-Process -Examples
Get-Help Get-Process -Full
Get-Alias
Get-Member -InputObject (Get-Process | Select-Object -First 1)
#endregion

#region Navigation und Dateien
Get-Location
Get-ChildItem
Set-Location ..
Get-Content -LiteralPath .\hosts.txt
Import-Csv -LiteralPath .\computers.csv
Get-FileHash -LiteralPath .\TrustedScript.ps1 -Algorithm SHA256
#endregion

#region Objektpipeline
Get-Process |
    Where-Object CPU -GT 10 |
    Sort-Object CPU -Descending |
    Select-Object -First 10 Name, Id, CPU

Get-Service |
    Where-Object Status -EQ 'Running' |
    Select-Object Name, Status, StartType
#endregion

#region Typen, Sammlungen und Operatoren
[string]$computerName = 'server01'
[int]$retryCount = 3
[bool]$enabled = $true

$computers = 'server01', 'server02', 'server03'
$computers[0]
$computers[-1]
$computers.Count

$settings = @{
    ComputerName = 'server01'
    Count        = 1
    Quiet        = $true
}

$result = [pscustomobject]@{
    ComputerName = 'server01'
    Online       = $true
    CheckedAt    = Get-Date
}

# Gleichheit und Vergleiche
5 -eq 5
5 -ne 3
10 -gt 3
10 -ge 10
2 -lt 5
5 -le 5

# Groß-/Kleinschreibung in Zeichenfolgen
'Admin' -eq 'admin'
'Admin' -ceq 'admin'

# Platzhalter und reguläre Ausdrücke
'server01' -like 'server*'
'server-042' -match '^server-\d{3}$'
'user@example.com' -replace '@.*$', '@redacted'

# Sammlungen
$roles = 'User', 'Analyst', 'Admin'
$roles -contains 'Admin'
'Admin' -in $roles

# Logik
($enabled -eq $true) -and ($retryCount -gt 0)
($computerName -eq 'server01') -or ($computerName -eq 'server02')
-not (Test-Path .\missing.txt)

# Arithmetik
$retryCount++
10 % 2
#endregion

#region Variablen, Bedingungen und Schleifen
$websites = 'google.com', 'microsoft.com'

foreach ($site in $websites) {
    $online = Test-Connection -ComputerName $site -Count 1 -Quiet

    if ($online) {
        "$site is online"
    }
    else {
        "$site is unreachable"
    }
}

for ($number = 0; $number -le 5; $number++) {
    "Number is $number"
}
#endregion

#region Pipeline und aktuelles Objekt
Get-Process |
    Where-Object { $_.CPU -gt 10 } |
    Select-Object Name, Id, CPU

Get-Service | ForEach-Object {
    [pscustomobject]@{
        Name    = $_.Name
        Running = $_.Status -eq 'Running'
    }
}
#endregion

#region Für die Pipeline geeignete Funktion
function Test-HostOnline {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory, ValueFromPipeline)]
        [ValidateNotNullOrEmpty()]
        [string[]]$ComputerName
    )

    process {
        foreach ($computer in $ComputerName) {
            [pscustomobject]@{
                ComputerName = $computer
                Online       = Test-Connection -ComputerName $computer -Count 1 -Quiet
                CheckedAt    = Get-Date
            }
        }
    }
}

'server01', 'server02' | Test-HostOnline
#endregion

#region Fehler
try {
    Get-Service -Name 'DefinitelyMissing' -ErrorAction Stop
}
catch {
    Write-Warning $_.Exception.Message
}
#endregion

#region Execution Policy und Signaturen
Get-ExecutionPolicy -List

# Nur bewusst ändern; CurrentUser erfordert normalerweise keine Administratorrechte.
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

Get-AuthenticodeSignature -FilePath .\TrustedScript.ps1
#endregion

#region REST-API
$apiParameters = @{
    Method      = 'Get'
    Uri         = 'https://api.github.com/repos/PowerShell/PowerShell'
    Headers     = @{ Accept = 'application/vnd.github+json' }
    ErrorAction = 'Stop'
}

try {
    Invoke-RestMethod @apiParameters |
        Select-Object full_name, stargazers_count, open_issues_count
}
catch {
    Write-Error "API request failed: $($_.Exception.Message)"
}
#endregion

#region Active Directory — nur in einer autorisierten Umgebung mit RSAT
# Import-Module ActiveDirectory
# Get-ADUser -Filter * -Properties Enabled, LastLogonDate
# Get-ADComputer -Filter * -Properties OperatingSystem
# Add-ADGroupMember -Identity 'Security Analysts' -Members 'alice' -WhatIf
# Disable-ADAccount -Identity 'alice' -WhatIf
#endregion

#region Gruppenrichtlinien — zuerst mit einer Test-OU arbeiten
# Import-Module GroupPolicy
# Get-GPO -All
# Get-GPOReport -All -ReportType Html -Path .\All-GPOs.html
#endregion

#region Diagnose und Auditierung
Start-Transcript -Path .\PowerShell-session.log
Write-Verbose 'Diagnostic message' -Verbose
Stop-Transcript

# Wenn PSScriptAnalyzer installiert ist:
# Invoke-ScriptAnalyzer -Path .\Script.ps1
#endregion

#region Git
# git status
# git diff
# git add .
# git commit -m "Describe the change"
# git pull --rebase
# git push
#endregion

}
